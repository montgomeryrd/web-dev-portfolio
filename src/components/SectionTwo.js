import React from 'react';
import './Sections.css';

const SectionTwo = ({ styling }) => {
    return (
        <div className={styling}>
            <section className="section-2"></section>
        </div>
    )
}

export default SectionTwo;